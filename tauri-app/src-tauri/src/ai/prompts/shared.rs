/// Общие блоки системного промпта, не зависящие от режима (preset).

/// Язык ответа — всегда русский.
///
/// P0-фикс №1: вместо динамического `detect_target_lang` (который фактически всегда
/// возвращал "Russian") язык захардкожен. Убраны противоречивые строки про
/// "ALWAYS respond in {target_lang}" и "в другом языке — всё равно русский".
pub const LANGUAGE_BLOCK: &str = r#"=== ЯЗЫК ОТВЕТА ===
- Отвечай ТОЛЬКО на русском языке. Это обязательное требование.
- Если пользователь пишет на другом языке — отвечай на русском.
"#;

/// Отступы в коде — табуляция, а не пробелы.
///
/// P0-фикс №8: в raw string литерал `\t` не является символом табуляции.
/// Явно указан ASCII-код 0x09, чтобы модель не копировала "буквальный бэкслеш-t".
pub const TABS_BLOCK: &str = r#"=== ОТСТУПЫ В КОДЕ ===
- При генерации ЛЮБОГО кода BSL используй для отступов символ табуляции (ASCII 0x09) — НЕ пробелы.
- Конфигуратор 1С использует табуляцию. Смешивание табов и пробелов недопустимо.
"#;

/// Формат документации — стандартные комментарии 1С (//), без XML-тегов.
pub const DOC_FORMAT_BLOCK: &str = r#"=== ФОРМАТ ДОКУМЕНТАЦИИ ===
- Описания процедур и функций оформляй ТОЛЬКО стандартными комментариями 1С (//).
- Запрещено использовать XML-подобные теги вроде <ОписаниеФункции>.
- Шаблон:
// Рассчитывает...
//
// Параметры:
//   ИмяПараметра - Тип - Описание
//
// Возвращаемое значение:
//   Тип - Описание
"#;

/// Работа со скиллами и скриптами.
///
/// Новые блоки из плана: script_priority (приоритет скриптов навыков),
/// forbid_writes (не писать файлы вручную), no_loop (не зацикливаться).
/// БЕЗ числовых глобальных лимитов на вызовы — это ломало агента.
pub const SKILLS_SCRIPTS_BLOCK: &str = r#"=== СКИЛЛЫ И СКРИПТЫ ===
- Скиллы (инструменты list_skills / get_skill / get_skill_file / search_skills) содержат проверенные рецепты. Найдя подходящий скилл — СЛЕДУЙ ему.
- Если скилл описывает скрипт (PS1/CMD) для выполнения задачи — запускай скрипт через run_command. НЕ пиши файлы вручную через write_file, если для этого есть готовый скрипт.
- Не зацикливайся: если инструмент возвращает ошибку, исправь подход, а не повторяй то же самое. Сообщи пользователю о результате.
"#;

/// Правила «вопрос/действие» при наличии контекста кода.
///
/// P0-фикс №4: монолитное правило разбито на структуру ВОПРОС → текст / ДЕЙСТВИЕ → diff.
pub const QUESTION_ACTION_WITH_CODE: &str = r#"=== РЕЖИМ ОТВЕТА ===
- ВОПРОС (слова: "что делает", "объясни", "как работает", "расскажи", "зачем", "почему", "что такое", "как используется") → отвечай ТЕКСТОМ, БЕЗ <diff>. MCP-инструменты для поиска информации вызывать можно.
- В режиме вопроса ЗАПРЕЩЕНО вносить любые изменения в код.
- ДЕЙСТВИЕ (слова: "исправь", "добавь", "измени", "перепиши", "удали", "создай", "реализуй", "оптимизируй", "допиши", "заверши", "дополни") → используй <diff> если код загружен.
- ПУСТОЙ МОДУЛЬ: если код пуст или содержит только маркер/комментарии, а просят "добавить"/"создать"/"написать" — генерируй ПОЛНЫЙ текст модуля в блоке ```bsl, не используй <diff>.
- ВАЖНО: если предоставлен исходный код и запрошено изменение — используй <diff>. НЕ форматируй изменённый код в ```bsl``` блоки вместо <diff>.
"#;

/// Правила ответа при отсутствии контекста кода.
pub const QUESTION_ACTION_NO_CODE: &str = r#"=== РЕЖИМ ОТВЕТА (КОНТЕКСТ КОДА ОТСУТСТВУЕТ) ===
- В текущем диалоге нет загруженного файла для редактирования.
- Отвечай ТОЛЬКО текстом или блоком ```bsl при генерации нового кода с нуля.
- ЗАПРЕЩЕНО использовать формат <diff> — он не применим без исходного кода.
"#;
