# 1c-search MCP Server

Высокопроизводительный MCP-сервер для поиска и навигации по исходным кодам конфигураций 1С:Предприятие (выгрузка в файлы). Оптимизирован для работы с большими конфигурациями (20 ГБ+, 100к+ файлов) даже на медленных HDD.

## 🛠 Инструменты

### Поиск по коду

| Инструмент | Описание |
|---|---|
| `search_code` | Полнотекстовый поиск по `.bsl` и `.xml` файлам. Поддержка регулярных выражений и ограничения области поиска через `scope` (`CommonModule.МодульИмя`, `Catalog.СправочникИмя` и т.д.) |
| `get_file_context` | Код вокруг указанной строки файла (±40 строк по умолчанию) |

### Поиск символов

| Инструмент | Описание |
|---|---|
| `find_symbol` | Найти процедуру/функцию по имени в индексе. Поддержка точного и подстрочного поиска |
| `get_symbol_context` | Полный код функции по файлу и номеру строки |
| `smart_find` | Умный поиск: находит символ + возвращает полный код за **один вызов**. Используй вместо `search_code` когда знаешь имя функции |
| `find_function_in_object` | Найти функцию внутри конкретного объекта 1С (справочник, документ, общий модуль) |
| `get_module_functions` | Список всех процедур и функций модуля. Принимает `CommonModule.Имя`, путь или просто имя модуля |

### Метаданные конфигурации

| Инструмент | Описание |
|---|---|
| `list_objects` | Список объектов конфигурации с фильтрами по типу и имени (справочники, документы, общие модули и т.д.) |
| `get_object_structure` | Полная структура объекта: реквизиты, табличные части, формы, команды, модули |

### Анализ зависимостей

| Инструмент | Описание |
|---|---|
| `find_references` | Все вхождения символа в коде конфигурации |
| `impact_analysis` | Анализ влияния: какие модули используют данный объект или символ |
| `get_function_context` | Граф вызовов: что вызывает функция и кто её вызывает |

### Служебные

| Инструмент | Описание |
|---|---|
| `stats` | Статистика индекса: количество символов, файлов, объектов, рёбер графа вызовов |
| `benchmark` | Замер производительности всех инструментов: min/avg/p95/max |
| `sync_index` | Принудительная инкрементальная синхронизация индекса |

---

## 🏗 Архитектура и алгоритмы

### Индексация символов

- **Tree-sitter парсер** ([tree-sitter-bsl](https://github.com/alkoleft/tree-sitter-bsl)) — точное извлечение AST: границы процедур, имена, флаг `Экспорт`, игнорирование комментариев и строковых литералов.
- **SQLite с B-Tree индексами** — мгновенный поиск по миллионам записей. Режим WAL позволяет читать и писать индекс одновременно.
- **Параллельная индексация** через [Rayon](https://github.com/rayon-rs/rayon) — задействует все ядра CPU.
- **Инкрементальная синхронизация** — хранит `mtime` каждого файла, перепарсирует только изменённые модули.

### Поиск

- **Двухфазный поиск символов**: Фаза 1 — prefix match по индексу SQLite (O(log n), мгновенно). Фаза 2 — substring scan, только если нужно добрать результаты.
- **Index-Guided Code Search**: при полнотекстовом поиске сначала опрашиваются «горячие» файлы из индекса символов — результат выдаётся до завершения полного скана.
- **Scope-фильтрация**: поиск можно ограничить конкретным объектом (`CommonModule.УчетНДС`) без скана всей конфигурации.
- **Time Budgeting**: операции поиска ограничены дедлайном 8 секунд.
- **Кодировки**: корректная обработка UTF-8 (с BOM), автоматический fallback на Windows-1251.

### Граф вызовов (Call Graph)

Строится при индексации: Tree-sitter разбирает каждый вызов внутри тел процедур/функций и записывает рёбра `caller → callee` в SQLite. Используется инструментами `get_function_context`, `impact_analysis`, `find_references`.

---

## ⚡ Оптимизации

1. **Инкрементальная синхронизация** — только изменённые файлы при повторном запуске.
2. **Максимальный параллелизм** — Rayon распараллеливает парсинг на все ядра.
3. **Streaming & Early Exit** — поиск останавливается, как только набрано нужное количество результатов (`limit`).
4. **SQLite WAL Mode** — чтение и запись индекса без блокировок.
5. **LTO + strip** — бинарник собирается с link-time оптimization и стрипингом символов.

---

## 🛠 Технологический стек

| | |
|---|---|
| **Rust** | Ядро сервера |
| **SQLite (rusqlite bundled)** | Хранилище символьного индекса и метаданных |
| **Tree-sitter + tree-sitter-bsl** | Парсинг BSL (инкрементальный AST) |
| **ignore (WalkBuilder)** | Рекурсивный обход файлов (та же библиотека, что в ripgrep) |
| **Rayon** | Параллельная индексация |
| **Tokio** | Async runtime для MCP stdio протокола |

---

## ⚙️ Настройка

Переменная окружения:

- `ONEC_CONFIG_PATH` — абсолютный путь к директории с исходниками конфигурации (выгрузка «Конфигуратор → Выгрузить конфигурацию в файлы»).

---

## 📊 Производительность

Замеры на конфигурации ERP 10.8 ГБ, 123к. файлов, 642 845 символов, SSD.

### Бенчмарк инструментов

Символ: `КоллекцияТабличныхЧастейТоваров`, n=20 итераций.

| Инструмент | Мин | Среднее | P95 | Макс |
|---|---:|---:|---:|---:|
| `find_symbol` — точный | 1 мс | 1 мс | 1 мс | 1 мс |
| `find_symbol` — по префиксу | 18 мс | 19 мс | 23 мс | 23 мс |
| `get_function_context` | 3 мс | 3 мс | 5 мс | 5 мс |
| `get_module_functions` | <1 мс | <1 мс | <1 мс | <1 мс |
| `list_objects` | 1 мс | 1 мс | 1 мс | 1 мс |
| `stats` | <1 мс | <1 мс | 1 мс | 1 мс |
| `get_file_context` | <1 мс | 1 мс | 19 мс | 19 мс |
| `search_code` (полнотекстовый) | 77 мс | 77 мс | — | 77 мс |

### Пример реальной сессии

Модель: qwen3-coder-plus. Задача: *«Найди функцию получения значения справочника СтавкаНДС по перечислению СтавкаНДС»*

| Этап | Что происходило | Время |
|---|---|---:|
| ИИ (Turn 1) | Thinking → выбор инструмента `search_code` | 6.9 с |
| MCP `search_code` | Поиск по 642 845 символам, 14 результатов | 356 мс |
| ИИ (Turn 2) | Анализ результатов → вызов `get_symbol_context` | 5.1 с |
| MCP `get_symbol_context` | SQLite + чтение строк файла, полный код функции | 4 мс |
| ИИ (Turn 3–4) | Финальный ответ | ~11 с |
| **Итого** | | **~23 с** |

- **Итерации поиска**: 2 — функция найдена с первой попытки
- **Время MCP**: 360 мс (<2% от общего) — всё остальное занимает генерация ИИ

---

*Часть проекта [Mini AI 1C](https://github.com/hawkxtreme/mini-ai-1c)*
