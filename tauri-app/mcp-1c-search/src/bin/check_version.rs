use tree_sitter;

fn main() {
    println!("MIN_COMPATIBLE_LANGUAGE_VERSION: {}", tree_sitter::MIN_COMPATIBLE_LANGUAGE_VERSION);
    println!("LANGUAGE_VERSION: {}", tree_sitter::LANGUAGE_VERSION);
}
